# product-group-indexer
## Izmantotās Python bibliotēkas:
- `pip install requests`
- `pip install lxml`
- `pip install fake-useragent`
### Var arī instalēt izmantotās versiajs ar:
- `pip install -r requirements.txt`